// Functions
function greet(name) {
  return `Hello, ${name}!`;
}

const sum = (a, b) => a + b;

// Advanced function usage
const complexFunction = ({ name, age }) => {
  return `Name: ${name}, Age: ${age}`;
};
